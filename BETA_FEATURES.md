# ✨ תכונות גרסת בטא - v0.0.2

## 🎯 מה חדש בגרסה זו

### 1. הודעת בטא דינמית
- ✅ באנר בראש כל דף עם אזהרה שזו גרסת בטא
- ✅ הצגת IP של המשתמש
- ✅ זיהוי מכשיר אוטומטי (iOS/Android/Desktop)
- ✅ זיהוי דפדפן
- ✅ מספר גרסה: **v0.0.2**
- ✅ כפתור מהיר לטופס צור קשר

### 2. טופס צור קשר מלא
- ✅ דיווח על באגים
- ✅ שאלות כלליות
- ✅ משוב
- ✅ שמירה אוטומטית של מידע טכני (IP, מכשיר, דפדפן, URL)
- ✅ אדמין יכול לראות את כל ההודעות

### 3. תיקון בעיית התבניות
- ✅ 20 תבניות מוכנות במיגרציה
- ✅ תיקון RLS לאפשר טעינה
- ✅ מחיקה וטעינה מחדש במידת הצורך

---

## 🚀 הפעלת התכונות החדשות

### שלב 1: הרץ את המיגרציה החדשה

הקובץ `supabase/migrations/20251018214500_add_contact_and_fix_templates.sql` כולל:
- יצירת טבלת `contact_messages`
- תיקון RLS של טבלת `templates`
- הוספת 20 תבניות

**דרך Supabase Dashboard:**
1. פתח: https://supabase.com/dashboard/project/agzeqhesddulsetepgcc
2. SQL Editor → New query
3. העתק את התוכן מהקובץ למעלה
4. Run

**או דרך CLI:**
```bash
supabase db push
```

### שלב 2: בדוק שהכל עובד

```bash
# בדוק תבניות
node -e "
const { createClient } = require('@supabase/supabase-js');
const supabase = createClient(
  'https://agzeqhesddulsetepgcc.supabase.co',
  'YOUR_ANON_KEY'
);

(async () => {
  const { data, count } = await supabase
    .from('templates')
    .select('*', { count: 'exact' });
  console.log('✅ Templates:', count);

  const { data: tables } = await supabase
    .from('contact_messages')
    .select('count');
  console.log('✅ Contact table exists');
})();
"
```

---

## 📋 מבנה טבלת contact_messages

```sql
CREATE TABLE contact_messages (
  id uuid PRIMARY KEY,
  user_id uuid (nullable),
  name text,
  email text,
  type text (bug/question/feedback),
  subject text,
  message text,
  device_info jsonb {
    ip: string,
    device: string,
    browser: string,
    userAgent: string
  },
  page_url text,
  referrer text,
  status text (new/in_progress/resolved/closed),
  created_at timestamptz,
  updated_at timestamptz
);
```

---

## 🎨 איפה הבאנר מופיע

הבאנר מופיע באופן אוטומטי בדפים הבאים:
- ✅ דף הבית (`/`)
- ✅ דשבורד (`/dashboard`)
- ⚠️ ניתן להוסיף לדפים נוספים לפי הצורך

**כיצד להוסיף לדף נוסף:**
```tsx
import { BetaBanner } from '../components/BetaBanner';

export function MyPage() {
  return (
    <div>
      <BetaBanner />
      {/* rest of page */}
    </div>
  );
}
```

---

## 💬 טופס צור קשר

### גישה לטופס:
```
/contact
```

### תכונות:
1. **3 סוגי פניות:**
   - 🐛 דיווח על באג (אדום)
   - ❓ שאלה (כחול)
   - 💬 משוב כללי (ירוק)

2. **מידע נשמר אוטומטית:**
   - IP כתובת
   - סוג מכשיר
   - דפדפן
   - URL הנוכחי
   - Referrer
   - User ID (אם מחובר)

3. **הצגת מידע טכני למשתמש:**
   - המשתמש רואה את המידע שיישלח
   - שקיפות מלאה

---

## 🔐 הרשאות אדמין

### צפייה בהודעות צור קשר:

```sql
-- אדמין רואה את כל ההודעות
SELECT * FROM contact_messages
ORDER BY created_at DESC;
```

### בעתיד: פאנל אדמין להודעות
ניתן להוסיף כרטיסייה חדשה בפאנל האדמין:
- רשימת כל ההודעות
- סינון לפי סוג (bug/question/feedback)
- סינון לפי סטטוס
- אפשרות לסמן כטופל/נפתר

---

## 🎯 חוויית משתמש

### הבאנר:
- ✅ צבעים בולטים (כתום-אדום גרדיאנט)
- ✅ סגירה זמנית (שומר ב-sessionStorage)
- ✅ כפתור "צור קשר" בולט
- ✅ מידע על המכשיר בזמן אמת

### הטופס:
- ✅ עיצוב נקי ופשוט
- ✅ אינדיקציות ויזואליות (אייקונים לכל סוג)
- ✅ הודעת אישור לאחר שליחה
- ✅ אפשרות לשלוח הודעה נוספת

---

## 📊 סטטיסטיקות בטא

ניתן לעקוב אחר:
- כמה באגים דווחו
- כמה שאלות נשאלו
- הבעיות הנפוצות ביותר
- מכשירים/דפדפנים עם הכי הרבה בעיות

**דוגמה לשאילתה:**
```sql
-- באגים לפי מכשיר
SELECT
  device_info->>'device' as device,
  COUNT(*) as bug_count
FROM contact_messages
WHERE type = 'bug'
GROUP BY device_info->>'device'
ORDER BY bug_count DESC;
```

---

## 🔧 התאמות אישיות

### שינוי גרסה:
בקובץ `src/components/BetaBanner.tsx`:
```tsx
const [deviceInfo, setDeviceInfo] = useState({
  ip: 'טוען...',
  device: '',
  version: '0.0.2'  // ← שנה כאן
});
```

### שינוי צבעי הבאנר:
```tsx
<div className="bg-gradient-to-r from-yellow-400 via-orange-400 to-red-400">
  // ← שנה כאן
```

### הסרת הבאנר לגמרי:
פשוט הסר את `<BetaBanner />` מהדפים.

---

## ✅ בדיקות לביצוע

- [ ] הבאנר מופיע בדף הבית
- [ ] הבאנר מופיע בדשבורד
- [ ] כפתור "צור קשר" עובד
- [ ] טופס צור קשר עובד
- [ ] הודעות נשמרות במסד הנתונים
- [ ] 20 תבניות זמינות באשף
- [ ] IP המשתמש מוצג נכון
- [ ] זיהוי מכשיר עובד
- [ ] אפשר לסגור את הבאנר

---

## 🐛 באגים ידועים

יתכנו באגים בגרסת בטא זו:
- בחירת תבנית עשויה לא להישמר (בטיפול)
- IP לא נטען במכשירים מסוימים (תלוי ב-CORS)
- הבאנר עשוי לחסום תוכן במובייל (לתיקון)

**דווח על באגים ב:** `/contact`

---

**גרסה: 0.0.2 Beta**
**תאריך עדכון: 18 אוקטובר 2025**
